# # Frontend production Dockerfile with NGINX

# FROM node:16 AS build

# WORKDIR /app

# COPY package*.json ./
# RUN npm install

# COPY . .
# RUN npm run build

# FROM nginx:alpine
# COPY --from=build /app/build /usr/share/nginx/html

# # Expose port 80 for the production app
# EXPOSE 80

# CMD ["nginx", "-g", "daemon off;"]


# Use an official node image as the base image
FROM node:14 as build

# Set the working directory
WORKDIR /app

# Copy package.json and package-lock.json to the working directory
COPY package.json package-lock.json ./

# Install dependencies
RUN npm install

# Copy the rest of the application code
COPY . .

# ARG for the backend URL (passed during build time)
ARG REACT_APP_BACKEND_URL

# Build the React app, with the backend URL embedded
RUN REACT_APP_BACKEND_URL=$REACT_APP_BACKEND_URL npm run build

# Use an Nginx image to serve the built files
FROM nginx:alpine

# Copy the build files to the Nginx HTML directory
COPY --from=build /app/build /usr/share/nginx/html

# Expose port 80
EXPOSE 80

# Start Nginx
CMD ["nginx", "-g", "daemon off;"]
